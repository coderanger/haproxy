<%= @proxy.proxy_type %> <%= @proxy.name %>
  <% if @proxy.appsession -%>
  <%= @proxy.appsession.to_cfg %>
  <% end -%>
  <% if @proxy.bind -%>
  <% @proxy.bind.each do |bind| -%>
  <%= bind.to_cfg %>
  <% end -%>
  <% end -%>
